name: Tests the creation of a location

steps:

  - description: POST a new location
    request:
      path: /locations
      method: POST
      data:
        #Todo: Put in data after endpoint is completed
#    extract:
#      event_id: '{response.event_id}'
#      user_id: '{response.user_id}'
#    validate:
#      - expect: 1
#        actual: '{response.event_id}'
#      - expect: 1
#        actual: '{response.user_id}'
#      - expect: 0
#        actual: '{response.status}'

  - description: POST an already existing location
    request:
      path: /locations
      method: POST
      data:
      # tODO: Put in data after endpoint is complete
